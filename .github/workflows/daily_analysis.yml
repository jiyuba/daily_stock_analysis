name: 每日股票分析

on:
  # 定时触发 - 每天北京时间 18:00 (UTC 10:00)
  schedule:
    - cron: '0 10 * * 1-5'  # 周一到周五，UTC 10:00 = 北京时间 18:00
  
  # 手动触发
  workflow_dispatch:
    inputs:
      mode:
        description: '运行模式'
        required: true
        default: 'full'
        type: choice
        options:
          - full          # 完整分析（股票+大盘）
          - market-only   # 仅大盘复盘
          - stocks-only   # 仅股票分析

jobs:
  analyze:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 安装依赖
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 创建必要目录
        run: |
          mkdir -p data logs reports
      
      - name: 执行股票分析
        env:
          # Gemini AI
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: ${{ secrets.GEMINI_MODEL || 'gemini-3-flash-preview' }}
          GEMINI_MODEL_FALLBACK: ${{ secrets.GEMINI_MODEL_FALLBACK || 'gemini-2.5-flash' }}
          GEMINI_REQUEST_DELAY: '3.0'  # GitHub Actions 建议增加延时
          
          # 数据源 (可选)
          TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
          
          # 搜索服务
          TAVILY_API_KEYS: ${{ secrets.TAVILY_API_KEYS }}
          SERPAPI_API_KEYS: ${{ secrets.SERPAPI_API_KEYS }}
          
          # OpenAI 兼容 API（备选）
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
          
          # 通知渠道（可同时配置多个，全部推送）
          # 方式一：企业微信
          # WECHAT_WEBHOOK_URL: ${{ secrets.WECHAT_WEBHOOK_URL }}
          # 方式二：飞书
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          # 方式三：Telegram（需同时配置 Bot Token 和 Chat ID）
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # 方式四：邮件（只需邮箱和授权码，SMTP自动识别）
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECEIVERS: ${{ secrets.EMAIL_RECEIVERS }}
          # 方式五：自定义 Webhook（支持钉钉、Discord、Slack、Bark等，多个用逗号分隔）
          CUSTOM_WEBHOOK_URLS: ${{ secrets.CUSTOM_WEBHOOK_URLS }}
          # 方式六：飞书文档
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_FOLDER_TOKEN: ${{ secrets.FEISHU_FOLDER_TOKEN }}

          
          # 自选股列表 (从 secrets 或使用默认值)
          STOCK_LIST: ${{ secrets.STOCK_LIST || '600519' }}
          
          # 其他配置
          LOG_LEVEL: INFO
          DATA_DAYS: 60
          MAX_CONCURRENT: 3
        run: |
          # 判断运行模式
          MODE="${{ github.event.inputs.mode || 'full' }}"
          
          echo "=========================================="
          echo "运行模式: $MODE"
          echo "自选股: $STOCK_LIST"
          echo "时间: $(TZ='Asia/Shanghai' date '+%Y-%m-%d %H:%M:%S')"
          echo "=========================================="
          
          if [ "$MODE" = "market-only" ]; then
            python main.py --market-review
          elif [ "$MODE" = "stocks-only" ]; then
            python main.py --no-market-review
          else
            python main.py
          fi
      
      - name: 上传分析报告
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: analysis-reports-${{ github.run_number }}
          path: |
            reports/
            logs/
          retention-days: 30
          overwrite: true  # 确保可以覆盖同名Artifact
      
      - name: 显示运行结果
        if: always()
        run: |
          echo "=========================================="
          echo "分析完成"
          echo "=========================================="
          if [ -d “reports” ]; then
            echo “生成的报告:”
            ls -la reports/
          fi

       - name: 通过企业微信应用发送通知
  if: always()
  env:
    JOB_STATUS: ${{ job.status }}
    MODE: ${{ github.event.inputs.mode || 'full' }}
    RUN_NUMBER: ${{ github.run_number }}
    RUN_ID: ${{ github.run_id }}
    SERVER_URL: ${{ github.server_url }}
    REPOSITORY: ${{ github.repository }}
    AGENT_ID: ${{ secrets.WECHAT_AGENT_ID }}
    CORP_ID: ${{ secrets.WECHAT_CORP_ID }}
    AGENT_SECRET: ${{ secrets.WECHAT_AGENT_SECRET }}
  run: |
    echo "正在获取企业微信 Access Token..."
    RESPONSE=$(curl -s -G "https://qyapi.weixin.qq.com/cgi-bin/gettoken" --data-urlencode "corpid=$CORP_ID" --data-urlencode "corpsecret=$AGENT_SECRET")
    if [ $? -ne 0 ]; then echo "❌ 请求Token API失败"; exit 1; fi
    echo "API原始响应: $RESPONSE"
    TOKEN=$(echo "$RESPONSE" | python3 -c "import sys, json; d=json.load(sys.stdin); exit(1) if d.get('errcode', 0)!=0 else None; print(d.get('access_token', ''))")
    if [ $? -ne 0 ] || [ -z "$TOKEN" ]; then
      ERR_MSG=$(echo "$RESPONSE" | python3 -c "import sys, json; d=json.load(sys.stdin); print(d.get('errmsg', 'Unknown error'))")
      echo "❌ 获取Token失败: $ERR_MSG (请检查 CorpID 和 Secret)"
      exit 1
    fi
    echo "✅ Token 获取成功"
    
    # 构建消息内容
    if [ "$JOB_STATUS" = "success" ]; then COLOR="#008000"; else COLOR="#FF0000"; fi
    CURRENT_TIME=$(date '+%Y-%m-%d %H:%M:%S')
    CONTENT="## 📊 股票分析任务已完成\n**状态：** <font color=\"$COLOR\">$JOB_STATUS</font>\n**触发模式：** \`$MODE\`\n**运行编号：** [$RUN_NUMBER]($SERVER_URL/$REPOSITORY/actions/runs/$RUN_ID)\n**完成时间：** $CURRENT_TIME\n\n> 注：详细分析报告可在GitHub Actions的Artifacts中下载。"
    
    # 使用 Python 构建 JSON
    MESSAGE_DATA=$(python3 -c "import json; print(json.dumps({'touser': 'JiYinRui', 'msgtype': 'markdown', 'agentid': '$AGENT_ID', 'markdown': {'content': '''$CONTENT'''}}))")
    
    echo "正在发送消息..."
    SEND_RESPONSE=$(curl -s -X POST "https://qyapi.weixin.qq.com/cgi-bin/message/send?access_token=$TOKEN" -H "Content-Type: application/json" -d "$MESSAGE_DATA")
    
    echo "发送API响应: $SEND_RESPONSE"
    SEND_ERRCODE=$(echo "$SEND_RESPONSE" | python3 -c "import sys, json; print(json.load(sys.stdin).get('errcode', '-1'))")
    if [ "$SEND_ERRCODE" != "0" ]; then
      SEND_ERRMSG=$(echo "$SEND_RESPONSE" | python3 -c "import sys, json; print(json.load(sys.stdin).get('errmsg', 'Unknown error'))")
      echo "❌ 消息发送失败 (错误码: $SEND_ERRCODE): $SEND_ERRMSG"
      echo "请检查: 1. AgentId是否正确; 2. 应用是否已启用; 3. 收件人'JiYinRui'是否在应用可见范围内"
      exit 1
    else
      echo "✅ 企业微信消息发送成功！"
    fi
